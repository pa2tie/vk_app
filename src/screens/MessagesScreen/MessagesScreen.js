import React, { Component } from "react";
import { AppRegistry, Text, View } from "react-native";

export default class MessagesScreen extends Component {
  render() {
    return <View />;
  }
}

AppRegistry.registerComponent("MessagesScreen", () => MessagesScreen);
